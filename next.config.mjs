/** @type {import('next').NextConfig} */
const nextConfig = {
    // Optional: Add custom headers or output configurations here
    // experimental: {
    //     typedRoutes: true,
    // },
};

export default nextConfig;
